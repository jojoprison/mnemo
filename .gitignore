**/config.json
.DS_Store
.idea/

# Local Claude Code agent worktrees — never track
.claude/worktrees/

# Python bytecode cache (auto-generated when scripts run/compile — never source)
__pycache__/
*.pyc

# рабочие goal-state файлы — локальные, не в публичный плагин
docs/plans/
.claude/loop-gate
